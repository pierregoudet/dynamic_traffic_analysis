#pragma once

#include <stdio.h>

float calc_esp(float*, float);
float calc_esp_square(float* ,float);
float calc_mean(float*, float);
float calc_variance_esp(float* , float);
int calc_alloc(float*, float, float);
float calc_transmission_lost(float*, int*, float);

