#include <stiod.h>
#include <stdlib.h>

#include "tad.h"
#include "const.h"

int main(int argc, char ** argv)
{
  (void)argc;
  (void)argv;
  
  int i;
  struct dat_c* d;
  
  d = data_c_create();

  for(i = 0; i < TIME; i++)
    {
      d->mes[i] = 1;
    }

 for(i = 0; i < CAPCITY; i++)
    {
      d->occ[i] = 1;
      d->prob[i] = 1;
    }
 d->var = 0.0;
 d->std_dev= 0.0;

  data_c_clear(d);
