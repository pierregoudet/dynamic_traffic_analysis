#pragma once

#include <stdio.h>

float calc_esp(float*, int);
float calc_esp_square(float* ,int);
float calc_mean(int*, int);
float calc_variance_esp(int* , float);
int calc_alloc(float*, int, float);


