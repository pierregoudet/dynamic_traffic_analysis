#include <stdio.h>
#include <stdlib.h>

#define TIME 100
#define NB_VN 5

int main (in argc, char **argv)
{
  FILE* f = fopen(argv[1], w+);
  
  int flow; 
  int i = 0;

  for(i = 0; i <= TIME; i++)
    {
      flow = rand() % 10;
      fwrite(&flow , int, 1, f);
    }
  return 0;
}
